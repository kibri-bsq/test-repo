{
  "name": "@engine/aggregation-engine",
  "version": "0.2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@engine/contracts": "workspace:*",
    "@engine/engine-model": "workspace:*"
  },
  "devDependencies": { "vitest": "^2.1.4" },
  "scripts": { "build": "tsc -p tsconfig.json", "test": "vitest run" }
}
